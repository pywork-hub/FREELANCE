mutation ToggleFavorite($serviceSlug: String!) {
	toggleFavorite(serviceSlug: $serviceSlug) {
		user {
			id
			profile {
				email
				login
				avatarPath
			}
			favorites {
				slug
			}
			roles
		}
	}
}
