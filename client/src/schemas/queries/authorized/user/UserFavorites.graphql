query UserFavorites($query: ServiceQueryInput!) {
	userFavorites(query: $query) {
		services {
			name
			slug
			fromPrice
			fromSalePrice
			fromTerm
			excerpt
			coverPath
			videoPath
			categories {
				name
				slug
			}
		}
		count
	}
}
